# RTK — Ticketing System

Every agent shell command must use `rtk`. See `.cursor/rules/rtk.mdc`.

```powershell
rtk git status
rtk dotnet build src/Ticket/Ticket.slnx
```

Primary build target: `src/Ticket/Ticket.slnx` (Backend Guide layout).
